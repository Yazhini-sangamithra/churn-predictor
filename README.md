# churn-predictor
tack web application. The project uses a trained classification model to predict customer churn based on input features. FastAPI handles backend API services including authentication and prediction endpoints, while Streamlit provides an interactive frontend dashboard.
